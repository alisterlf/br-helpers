This directory stores Changesets metadata files.

For user-facing changes, run `npm run changeset` and commit the generated markdown file with your code changes.
The release workflow uses these files to open an automated release PR that updates `package.json` and `CHANGELOG.md`.
